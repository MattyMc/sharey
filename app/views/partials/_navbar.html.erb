<!-- NAVBAR -->
<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" id="navbar-brand-img" href="<%= root_path %>">
        <%= image_tag "ShareyIcon_48x48_8.png", alt:"Sharey", class:"img-circle", id:"" %>
      </a>
      <a class="navbar-brand" href="<%= root_path %>">Sharey</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li <%= 'class=active' if current_page?(root_path) %>><a href="<%= root_path %>">Home <span class="sr-only">(current)</span></a></li>
        <li <%= 'class=active' if current_page?(my_friends_path) %>><a href="<%= my_friends_path %>">My Friends</a></li>
        <li <%= 'class=active' if current_page?(saved_items_path) %>><a href="<%= saved_items_path %>">Saved Items</a></li>
        <li <%= 'class=active' if current_page?(about_path) %>><a href="<%= about_path %>">About</a></li>
      </ul>

      <div class="navbar-right">
        <% if user_signed_in? %>
        <ul class="nav navbar-nav">
          <li><a href="#"><%= "Hello " + current_user.first_name + "!" %></a></li>
          <li><%= link_to 'Sign out', sign_out_path %></li>
        </ul>
        <% else %>
            <a class="btn btn-social btn-google-plus" href='/auth/google_oauth2'>
              <i class="fa fa-google-plus"></i>
              Sign in with Google
            </a>
        <% end %>
      </div>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container -->
</nav>